# 2026-08-06 · 德语术语校对回写（P9 收尾）

## 背景

P9 三语国际化的最后一步：德语校对表（`EU-TMS_三语术语校对表.xlsx`）已由 Frank
核对完毕发回（`_已核对` 版），本次把核对意见批量回写到四套德语语言包。

## 核对意见汇总（33 条）

### 术语定版（Sheet 3「重点确认」）

| 中文 | 定版德语 | 变更 |
|---|---|---|
| 报关行 | **Zollagentur** | 原 Zollagent，改 |
| 清关 | Verzollung | 保持 |
| LDM | LDM | 保持（不写全称 Lademeter） |
| 实重 | **Tatsächliches Gewicht** | 原 Realgewicht，改 |
| 按件货物明细 | Packstückliste | 保持 |
| 已签收 | Empfang bestätigt | 保持（与「已送达 Zugestellt」区分，见下方说明） |
| 拼车 LTL | Teilladung (LTL) | 保持（备注：Stückgut 口语也可表达零担） |
| 税号 | **USt-IdNr.** | 原 Steuernummer，改（要的是欧盟增值税识别号） |
| 银行账号 | **IBAN** | 原 Bankkonto/Kontonummer，改 |
| 货损 | Schaden | 保持 |
| 船司放单 | Reederei-Freistellung | 保持 |
| 客户管理员 | **Hauptbenutzer** | 原 Kundenadministrator，改 |
| 贵司单号 | **Ihre Auftragsnr.** | 原 Ihre Referenz，改 |
| 待接单 | **Offene Aufträge** | 原 Annahme offen/ausstehend，改（Zu bestätigen 弃用，易被读成客户已确认订单） |

### 逐条修改（Sheet 1/2）

清关中→In Verzollung、系统副标题→Europäisches Transportmanagementsystem、
件数→Packstücke、货物描述→Warenbeschreibung、
客户信用超额预警→Kreditlimit überschritten、
`{{count}} Auftrag/Aufträge unterwegs`→`{{count}} Aufträge unterwegs`、
通知渠道「站内」System→In-App，以及上表术语在各处的落地。

### 与 Frank 确认过的三处歧义

1. Sheet1 第 129 行「在途中」误填 `Kreditlimit überschritten` →
   实际改的是第 179 行「客户信用超额预警」；「在途中」保持 Unterwegs。
2. 「已签收」填了两个词（`Empfang bestätigt /Zugestellt`）→
   定为 **Empfang bestätigt**，保持与「已送达 Zugestellt」的区分。
3. Sheet1 第 44 行（GPS 追踪）填的 `USt-IdNr.`、第 55 行（我的账单）填的 `IBAN`
   确认为误填（是 Sheet3 税号/银行账号的决定串行了），忽略。

## 修改文件

| 文件 | 处数 | 说明 |
|---|---|---|
| `server/i18n/locales/de.json` | 1 | Excel 导出列「件数」 |
| `admin/src/i18n/locales/de.json` | 8 | 副标题、角色名、件数×2、报关行×2、信用预警、CLIENT_ADMIN 提示 |
| `customer-portal/src/i18n/locales/de.json` | 12 | 询价表格列、贵司单号、角色名/说明句、通知渠道等 |
| `carrier-portal/src/i18n/locales/de.json` | 10 | 待接单×4、税号×2、银行账号×2、清关中、CLIENT_ADMIN 提示 |

共 **31 处**，全部只动 `de.json` 的值；zh/en 语言包、代码零改动。
回写方式：脚本按（中文值 + 旧德语值）精确匹配后替换，先干跑核对全部命中再落盘，
四份 JSON 重新序列化后经 `json.load` 校验，diff 确认无格式噪音。

同类词全局统一（超出表格逐行范围但属于同一术语决定的）：
- `Zollagent eingeben` 占位符 → `Zollagentur eingeben`
- 承运商门户「税号/银行账号」的**标签**（表里只核了占位符）一并改 USt-IdNr. / IBAN
- `Kundenadministrator` 出现的角色名与 apiMessage 长句（三端）→ Hauptbenutzer
- 「件数 Anzahl」在运营端两处表格列与后端 Excel 导出列 → Packstücke
  （LDM 计算公式里的 `× Anzahl` 指数量，不在此列，未动）

## 影响分析

- 纯文案变更，无逻辑/接口/构建配置改动；德语界面显示即时生效（构建后）。
- 提示条成败判断早已按踩坑 032 改为 `{text, ok}` 结构，不依赖文案匹配，
  本次改文案不会破坏任何判断逻辑。
- 状态值仍是大写枚举（`ASSIGNED` 等），只改了显示文案，筛选/查询不受影响。

## 部署提醒（未做）

语言包在前端构建产物里，**需要重新构建三端并按部署规范上传**；
后端 `server/i18n` 那 1 处随后端下次发布生效。本次仅提交代码，未部署。

## 相关文档

- 核对原件：`docs/EU-TMS_三语术语校对表_已核对.xlsx`（已替换未核对版）
- 踩坑：`docs/踩坑经验库/041_Excel校对表回收不能只看修改列要全表diff.md`
