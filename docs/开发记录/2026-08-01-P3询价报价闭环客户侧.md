# P3 · 询价报价闭环·客户侧（需求 2 + 5.1 + 5.4）

**日期**：2026-08-01
**模块**：询价 / 报价 / 订单 / 客户门户
**状态**：⚠️ 代码完成并通过本地验证，**迁移 107 尚未在生产执行、尚未部署**

---

## 功能说明

打通「询价 → 报价 → 客户确认 → 自动生成订单」全链路。

### 1. 需求 2：客户接受报价即自动建单

老流程是两步且中间常年断档：客户接受（只改状态）→ 运营手工点「一键下单」。
现在 `POST /quotations/:id/accept` 内部直接建单：

| 服务类型 | 自动建出的订单初始状态 | 说明 |
|---------|----------------------|------|
| TRUCK_LTL / TRUCK_FTL | `PENDING_REVIEW` 待审核 | 正是需求 2 要的"自动进待审核" |
| LOCAL_DELIVERY | `PENDING_DISPATCH` 待派送 | 见下方「设计决策」 |

- 货物信息从来源询价单带进订单（旧的手工 convert-order 建出来的是空订单）
- `quotations.converted_order_id` 记住产出的订单，"建没建单"有唯一事实来源
- 建单失败（信用超额）整笔事务回滚，报价退回原状态 —— Frank 2026-08-01 拍板的口径
- 手工 `convert-order` 保留，收紧为仅运营可调，用于存量补建

### 2. 需求 5.1：询价三来源补齐前两种

| 来源 | 本次状态 |
|------|---------|
| 客户门户自助提交 | 表单从 5 个字段升级为结构化地址 + 联系人 + 按件明细 |
| 运营代客户建单 | **全新**，admin 此前完全没有询价页面 |
| 开放 API（易抵达等） | P8 再做，数据结构已按 `inquiry_cargo_items` 预留 |

### 3. 需求 5.4：按件明细 + 一键复制 + 导出

- 新表 `inquiry_cargo_items`：单号 / 件数 / 长宽高 / 单件实重 / 单件体积 / 行级 LDM
- **LDM = 长m × 宽m ÷ 2.4 × 件数**（2.4 = 欧洲标准车厢内宽），自动算、可勾「手改」覆盖
- 单件体积 = 长 × 宽 × 高 ÷ 1,000,000（长宽高单位 cm）
- 明细汇总回写询价表头的件数/实重/体积/LDM；**无明细行时不回写**，避免抹掉客户手填的总量
- 「复制摘要」：固定文本模板，含单号/国家/地址/邮编/电话/邮箱/件数/长宽高/实重/体积/LDM，直接可粘给服务商（清平姐的重点痛点，支持列表批量勾选）
- 「导出 Excel」：固定模板，一行一件货，表头字段每行重复，方便服务商筛选排序

### 4. 新增报价状态 PENDING_DECISION（客户待定）

客户在门户可选**接受 / 拒绝 / 待定**。待定后报价停在 `PENDING_DECISION`，
运营在列表和统计卡片上能一眼挑出需要跟进的单，客户之后仍可改为接受或拒绝。

---

## 设计决策（需要 Frank 知晓）

1. **本地派送自动建单落 `PENDING_DISPATCH` 而不是 `PENDING_REVIEW`。**
   P1 定的本地派送状态机初始状态是 `PENDING_QUOTE`（待报价），但报价已经被客户接受了，
   订单停在"待报价"自相矛盾。本地派送的简化流里没有"待审核"这一环，所以推进一格到待派送。
   **代价**：本地派送订单不经运营审核直接进待派送。如果业务上要保留审核关口，需要再议。

2. **询价地址不另开列，继续用 `route_from` / `route_to` JSONB**，
   形状固定为 `{ country, city, zipCode, address }`，与 `orders.pickup_address` 一致。
   这样前后端只有一套契约（踩坑 011 形态四就是两端形状不一致踩的）。

3. **admin 侧边栏仍是一个「询价报价」入口**，指向 `/inquiries`，
   页内用 `InquiryQuotationTabs` 在询价/报价两张列表间切换，`/quotes` 也点亮同一菜单项。

---

## 修改文件清单

### 数据库

| 文件 | 操作 | 说明 |
|------|------|------|
| `server/database/migrations/107_inquiry_quotation_loop.sql` | 新增 | inquiries 补联系人/客户单号/LDM；新表 inquiry_cargo_items；quotations 补客户决策留痕 + converted_order_id；存量 converted_order_id 回填 |

### 后端

| 文件 | 操作 | 说明 |
|------|------|------|
| `server/modules/inquiry/service.js` | 新增 | LDM/体积换算、明细整单替换、汇总回写、复制摘要文本生成 |
| `server/modules/inquiry/routes.js` | 重写 | 新增 `/stats`、`POST /summary`（批量）、`/export`（Excel）、`/:id/summary`；详情带明细与关联报价；**全部端点补客户身份越权校验**；创建/编辑支持按件明细与新字段 |
| `server/modules/quotation/routes.js` | 修改 | accept 自动建单 + 行锁防重复；新增 `/:id/pending` 待定；accept/reject/pending 加身份校验；**create/edit/send/void/new-version/calculate-price 收紧为仅运营**（此前客户账号也能创建和修改报价）；`GET /:id` 与 `/:id/versions` 补越权校验；列表加询价号/订单号/搜索；stats 补 pending_decision 并把 `conversionRate` 改为 `conversion_rate` |

### admin 运营管理端

| 文件 | 操作 | 说明 |
|------|------|------|
| `admin/src/constants/inquiryQuotation.ts` | 新增 | 询价/报价状态枚举、文案、样式、LDM 换算的唯一来源 |
| `admin/src/components/InquiryQuotationTabs.tsx` | 新增 | 询价/报价切换 |
| `admin/src/pages/InquiryManagement.tsx` | 新增 | 询价列表 + 统计 + 筛选搜索 + 勾选批量复制摘要 / 导出 Excel + 由此报价 |
| `admin/src/pages/InquiryDetail.tsx` | 新增 | 询价详情 + 按件明细表 + 关联报价 + 复制摘要 |
| `admin/src/pages/InquiryEdit.tsx` | 新增 | 代客户建询价 / 编辑，含按件明细录入与 LDM 实时计算 |
| `admin/src/pages/QuotationManagement.tsx` | 修改 | **修四处字段/路由错位（详见踩坑 018）**；状态 Tab 改用共享常量；加来源询价列与订单号；加客户待定统计与操作；接受后跳转新订单 |
| `admin/src/pages/QuotationCreate.tsx` | 修改 | 接收 `?inquiryId=`，拉询价预填客户/服务类型/路线，顶部显示来源询价提示条，提交时带 inquiryId |
| `admin/src/components/StatusBadge.tsx` | 修改 | 补 PENDING_DECISION / CONVERTED / QUOTED 的颜色与中文名 |
| `admin/src/App.tsx` | 修改 | 注册 4 条 `/inquiries` 路由（create 写在 :id 之前） |
| `admin/src/components/Sidebar.tsx` | 修改 | 菜单指向 `/inquiries`，`/quotes` 也点亮同一项 |
| `admin/src/components/Header.tsx` | 修改 | `/inquiries` 标题映射 |

### customer-portal 客户门户

| 文件 | 操作 | 说明 |
|------|------|------|
| `customer-portal/src/constants/inquiryQuotation.ts` | 新增 | 门户自己的一份状态与换算常量（子系统隔离，不跨端 import） |
| `customer-portal/src/pages/MyQuotations.tsx` | 新增 | 我的报价页，接受 / 拒绝 / 待定，接受即自动建单并引导去订单页 |
| `customer-portal/src/pages/InquiryList.tsx` | 重写 | 结构化地址 + 联系人 + 按件明细录入 + LDM 实时计算；**修小写状态 map**（踩坑 004 原案发地） |
| `customer-portal/src/App.tsx` | 修改 | 注册 `/quotations` 路由 |
| `customer-portal/src/components/Layout.tsx` | 修改 | 导航加「我的报价」 |

---

## 关联影响分析

- **顺带修掉的越权漏洞**：报价的 accept / reject 此前只挂 `authenticateToken`，**任何登录账号
  （含别家公司的客户账号、承运商账号）都能接受或拒绝任意一张报价**；create/edit/void 同理。
  本次全部补上身份校验。询价的 `GET /:id` / PUT / DELETE 也有同样问题，一并修复。
- **carrier-portal 不受影响**：承运商不参与询价报价流程，接口层已显式拒绝 CARRIER 身份。
- **订单模块**：只通过 `orderService.createOrder` / `updateStatus` 调用，**没有改动 order 模块任何文件**，
  与并行进行的 P2 订单文件中心不冲突。
- **信用管理**：复用 `createOrder` 内既有的信用检查，未改 credit-manager。
- **自动开票**：本地派送签收即 COMPLETED，行为不变。
- **确认过不受影响**：CMR / 清关 / 放单 / GPS / 财务均不读询价与报价表。

---

## 本地验证结果

| 验证项 | 结果 |
|--------|------|
| 全新空库顺序跑 100→107 | ✅ 无错误 |
| 107 重复执行（幂等） | ✅ 无错误 |
| LDM / 体积换算单元验证 | ✅ 120×80×100cm ×3件 → 体积 0.96 m³、LDM 1.2 |
| 明细汇总回写（真实 SQL） | ✅ 2 行明细 → 件数 5 / 实重 800kg / 体积 3.72m³ / LDM 2.3 |
| 明细清空不抹表头 | ✅ 表头实重保持 800 未被清零 |
| 接受报价自动建单（三种服务类型） | ✅ LTL/FTL → PENDING_REVIEW，本地派送 → PENDING_DISPATCH，货物信息已带入，报价挂上订单 |
| 迁移回填 converted_order_id | ✅ 命中已建单的存量报价 |
| Express 路由顺序（踩坑 001） | ✅ inquiry 9 条 / quotation 14 条，固定路径均在参数路径之前 |
| `npx tsc --noEmit`（踩坑 014） | ✅ admin 0 错误、customer-portal 0 错误 |
| `npm run build` | ✅ admin / customer-portal 均通过 |

> carrier-portal 有 2 个**存量** TS 错误（`import.meta.env` 类型声明缺失），本次未触碰该端。

---

## 上线步骤（尚未执行）

1. **备份 RDS**（迁移 107 会改表结构）
2. 在生产执行 `107_inquiry_quotation_loop.sql`
3. push 触发 CI/CD 部署后端 + admin + customer-portal
4. 生产验证：
   - 客户门户提交一张带按件明细的询价，检查表头 LDM 是否正确汇总
   - admin 询价列表能看到该单，「复制摘要」和「导出 Excel」正常
   - 「由此报价」→ 创建并发送报价
   - 客户门户「我的报价」点接受 → 检查是否自动生成 `PENDING_REVIEW` 订单、
     `quotations.converted_order_id` 是否写上
   - 用另一家公司的客户账号 curl 一次别家的报价 accept，应返回 403

---

## 遗留 / 后续

1. **单据流 amount 为 NULL**（踩坑 017）：`order/service.js` 里那句显式 `createFlowLink`
   因 `ON CONFLICT DO NOTHING` 从未生效，所有 QUO→ORD 连线的金额是空的。
   属于 order 模块既有行为，本次未动，建议单独修 `document-flow.js` 的 ON CONFLICT。
2. **报价邮件通知**是 P4 的内容：本次 `/:id/send` 只改状态，没有发邮件，
   询价单上收集的 `contact_email` 就是给 P4 用的收件人。
3. **本地派送跳过运营审核**（见「设计决策」第 1 条），需业务确认是否可接受。
4. 询价目前没有"作废"，只有待报价状态可删除；如需软删除要补 `void_reason`（参考踩坑 010）。
