# 2026-08-02 P8 第二阶段：admin 端开放 API 管理页面

> 状态：✅ 代码完成，本地冒烟 23 项全过 + tsc 零错误 → 待部署生产
> 模块：open-api（扩展）+ admin 前端
> 迁移：113（权限码）

## 功能说明

P8 第一阶段上线了开放 API 平台，但密钥管理只能 SSH 上服务器跑脚本。
本阶段把它搬进运营管理端：**系统设置 → 开放 API 对接**（`/settings/open-api`）。

- **密钥管理 tab**：签发（弹窗显示明文一次，带复制按钮）、编辑（名称/限速/IP 白名单/备注）、
  停用/启用（即时生效）、换钥匙（旧钥匙即刻失效，二次确认有明确后果提示）
- **请求日志 tab**：全部推送请求留痕查询，按合作方/结果/对方单号筛选，分页
- 权限码 `open_api:manage`（迁移 113），默认分配 sys_admin + op_manager；
  op_staff 刻意不给——密钥能以客户名义建单，等同"替客户下单"的钥匙

## 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `server/database/migrations/113_open_api_admin_permission.sql` | 新增 | open_api:manage 权限码 + sys_admin/op_manager 默认分配 |
| `server/modules/open-api/admin-routes.js` | 新增 | JWT 区管理端点：keys 列表/签发/编辑/停用/启用/rotate + logs 分页筛选 |
| `server/modules/open-api/service.js` | 修改 | 抽出 generateKey()，签发脚本与 admin 接口共用一处 |
| `server/app.js` | 修改 | +2 行：挂载 `/api/v1/open-api` |
| `server/scripts/create-api-key.js` | 修改 | 改为从 service.js 导入 generateKey/hashKey，删除本地重复实现 |
| `server/scripts/check-permission-menu-sync.js` | 修改 | 权限字典从"只读 109"改为扫描全部含 INSERT INTO permissions 的迁移（否则 113 的新码会被误报） |
| `server/scripts/test-open-api-admin.js` | 新增 | 管理端冒烟 23 项（只能对测试库跑） |
| `admin/src/pages/OpenApiManagement.tsx` | 新增 | 管理页面（密钥/日志两 tab，约 640 行——两个联动视图收在一页，职责单一暂不拆） |
| `admin/src/pages/Settings.tsx` | 修改 | ERP 管理入口加「开放 API 对接」卡片 |
| `admin/src/constants/permissions.ts` | 修改 | MENU_PERMISSIONS 加 `/settings/open-api` 映射（PermissionRoute 路由守卫随之生效） |
| `admin/src/App.tsx` | 修改 | lazy import + 路由 |
| `admin/src/components/Header.tsx` | 修改 | 页面标题映射 |

## 权限四件套核对

1. ✅ 权限定义：迁移 113
2. ✅ 菜单映射：MENU_PERMISSIONS `/settings/open-api` → `open_api:manage`
3. ✅ 页面鉴权：PermissionRoute 按 MENU_PERMISSIONS 拦路由（无权限直接进不了页面，页内不再重复判断）
4. ✅ 后端鉴权：admin-routes 整组 `authenticateToken + requireUserType('OPERATOR') + requirePermission('open_api:manage')`
5. ✅ `check-permission-menu-sync.js` 通过（exit 0）

## 关联影响分析

1. **对外 API（/api/open/v1）行为零改动**：service.js 只加了 generateKey 导出。
2. **create-api-key.js 脚本保留且可用**（冒烟里实测）：服务器上没有浏览器时仍可应急用。
3. **签发人即操作人**：admin 签发的钥匙 created_by 是当前登录运营，比脚本兜底的 sys_admin 语义更准（踩坑 019 的 resolveActingUser 顺序不变）。
4. **check-permission-menu-sync.js 改动向后兼容**：109 仍在扫描范围内，权限字典只增不减（顺带把 002_seed_data.sql 里的历史权限 INSERT 也纳入字典，只影响"未使用"参考列表，无害）。
5. **刻意不支持**：改 partner_code（幂等键的一半，历史单据会对不上）、换挂靠客户（等于把对方之后的单挂到另一家名下）——要换就停用旧钥匙重新签发。
6. **确认不受影响**：Sidebar 菜单不变（入口在系统设置卡片里，与 master-data 等同级）；客户/承运商门户零改动。

## 本地验证（2026-08-02，一次性测试库 eu_tms_p8b_test，跑完已删）

- 后端冒烟 23 项全过：鉴权 4（401/staff 403/manager 200/admin 200）、签发 5（校验/明文格式/
  重复 400/大写与 IP 清洗/响应无 key_hash）、对外联动 2（IP 白名单拦截与放行）、
  编辑 4（含清空备注存 NULL、空更新 400、404）、停启换 4（停用即时生效/旧 401 新 200）、
  日志 4（分页结构/按结果/按合作方筛选/staff 403）
- 改造后的 create-api-key.js 实测 list 正常
- check-permission-menu-sync.js exit 0
- admin `tsc --noEmit` 零错误（踩坑 014：vite build 不查类型，单独跑）+ `npm run build` 成功

## 生产验证清单（部署后执行）

- [ ] RDS 备份 → 执行迁移 113 → 确认 permissions 有 open_api:manage、sys_admin/op_manager 已分配
- [ ] admin 登录（sys_admin）：系统设置出现「开放 API 对接」卡片，页面能打开，日志 tab 能看到第一阶段生产验证留下的 401 记录
- [ ] 无权限角色（如 op_staff 账号）看不到卡片对应页面（直接访问 URL 被 PermissionRoute 拦）
- [ ] pm2 无启动报错
