# 2026-08-02 P8 第三阶段：开放 API 状态回查接口

> 状态：✅ 已上线（2026-08-03 00:40-00:44，提交 abad667，生产功能级验证通过）
> 模块：open-api（扩展，无数据库迁移）

## 功能说明

合作方推单后凭自己的 `externalOrderNo` 随时查进展，补齐"推得进来、查不到结果"的缺口：

| 端点 | 返回 |
|------|------|
| `GET /api/open/v1/inquiries/{externalOrderNo}` | 询价状态 + 最新一版客户可见报价（金额/币种/有效期）+ 已转订单号 |
| `GET /api/open/v1/orders/{externalOrderNo}` | 订单状态（含中文 label）、派送子状态、跟踪号、柜号、计划日期 |

规则：
- **404 语义租户隔离**（架构规则 8）：查不到和"单号是别人的"一律 404 NOT_FOUND，不暴露存在性
- **成本永不外泄**（踩坑 026）：报价点名列查询，carrier_cost 不进响应；冒烟里有插成本再回查的回归断言
- 报价只取 `status NOT IN (DRAFT, CANCELLED)` 的最新版本；NUMERIC 金额出门前转数字（踩坑 002）

## 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `server/modules/open-api/service.js` | 修改 | +getInquiryStatusForApi / getOrderStatusForApi；引入 order/service 的 getStatusLabel |
| `server/modules/open-api/routes.js` | 修改 | +2 个 GET 端点；头注释补路由顺序警示（以后加固定路径要放 /:param 前，踩坑 001） |
| `server/modules/open-api/middleware.js` | 修改 | 留痕的 external_ref 兼容路径参数（handler 挂 req.apiExternalRef） |
| `admin/src/pages/OpenApiManagement.tsx` | 修改 | 日志结果徽章 +NOT_FOUND（回查未命中） |
| `server/scripts/test-open-api.js` | 修改 | +8 项断言（回查/成本回归/404/跨合作方隔离/留痕） |
| `docs/对外API/EU-TMS开放API对接文档-v1.0.md` | 修改 | 新增第 7 节状态回查 + 404 错误码 + 修订记录（原 7/8 节顺延为 8/9） |

## 关联影响分析

1. **无数据库迁移**：只读查询，复用 112 的幂等索引（回查 WHERE 就是索引前缀，走索引）。
2. **推送端点行为零改动**；留痕中间件改动向后兼容（body 里的单号照旧记录）。
3. **对外契约变化是"新增"**：POST 端点字段未动，不影响合作方正在评审的字段清单；文档"需要贵方确认"第 2 条从"是否要轮询接口"改为"是否还要 Webhook"。
4. **确认不受影响**：admin 管理页除新增一个徽章文案外无逻辑改动；/api/v1 全部路由未动。

## 本地验证（2026-08-02，一次性测试库，跑完已删）

冒烟 39 项全过（原 31 + 新 8）：询价回查（未报价 quotation=null / 已报价金额可见且为数字 /
响应不含 carrier_cost 与成本值）、订单回查（状态+中文 label+柜号）、404 NOT_FOUND、
跨合作方查同单号 404、无钥匙 401、NOT_FOUND 留痕带路径参数单号。
admin tsc --noEmit 零错误。

## 生产验证（2026-08-03 00:40-00:44，全部通过）

- [x] CI run 30757165229 成功，新代码落盘（grep 命中），PM2 双实例重启 online
- [x] **功能级**：临时签发冒烟钥匙 P8SMOKE → `GET /orders/NOPE` 与 `GET /inquiries/NOPE` 均返回 JSON `404 NOT_FOUND`（老代码会落到 Express 默认 404 HTML，此结果证明新路由真实生效）、ping 200
- [x] 冒烟钥匙及其 3 条日志已定向删除（api_keys 归零，不留测试残留）

> 注意：无钥匙 401 **不能**作为新路由生效的证据——router.use(apiKeyAuth) 对任意路径都先拦，
> 老代码也返回 401。这次差点被它骗过，功能级验证必须带真钥匙。
