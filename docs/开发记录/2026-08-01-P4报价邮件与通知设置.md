# P4 · 报价邮件通知 + 客户通知设置（需求 5.2 + 8）

**日期**：2026-08-01
**模块**：报价 / 通知 / 系统设置 / 客户门户 / 定时任务
**状态**：✅ **已上线**（2026-08-01 23:53-23:58，备份 → 迁移 108 → push 9e3c3da → CI/CD 部署三端 → 生产验证通过）
**⚠️ 但邮件仍无法送达**：发信域名缺 SPF/DKIM，详见「遗留」第 1 条

---

## 功能说明

### 1. 需求 5.2：报价邮件 + 三个确认链接

PDF 原问"系统能否读客户回复邮件"。系统没有 IMAP 收信能力，
内容解析成本高且不可靠，按开发计划「四、待确认」第 2 条的建议改为
**邮件里放三个一次性确认链接**。

运营点「发送报价」时，同一笔事务内完成：改状态为已发送 → 签发一次性 token →
把邮件排进 P0 建好的邮件队列（cron 每 2 分钟真正发信）。

客户收到的邮件含订单基本信息、报价金额和三个按钮：**同意 / 待定 / 拒绝**。

| 安全设计 | 做法 |
|---------|------|
| token 强度 | 32 字节随机（256 位），base64url |
| 存储 | 只存 SHA-256 哈希，明文仅存在于那封邮件里 |
| 单次使用 | `used_at` 一旦写上即作废；并发点击靠 `WHERE used_at IS NULL` + 检查 rowCount 拦住 |
| 过期 | 默认取报价 `valid_until`（落到当天 23:59:59）；报价已过期则退回配置的天数 |
| 重发作废旧链接 | 再次发送时把该报价所有未使用 token 标记 `SUPERSEDED` |
| 防扫描器误触 | **GET 只渲染确认页，POST 才改状态** |
| 限流 | 免登录端点挂 20 次 / 60 秒，挡 token 枚举 |
| 追溯 | 记录 `sent_to` / `used_ip` / `used_action` |

> **为什么 GET 不能直接改状态**：Outlook / Gmail 的安全扫描器会预取邮件里的链接。
> 如果 GET 就落库，客户还没打开邮件，报价可能已经"被同意"并自动建单了。

### 2. 需求 8：付款提醒 + 逾期处理

每天 9:00 的定时任务重写：

- **自动把逾期账单标记为 `OVERDUE`**（此前完全没有，逾期账单一直显示"未付款"，财务页逾期统计永远是 0）
- **到期前 N 天提醒** + **逾期催款**，两者都发给客户本人（收件人取开票邮箱优先），不再只发内部站内信
- 天数与开关全部读 `system_settings`，运营在系统设置页可改
- 同一张账单同一天不重复发，避免 cron 重启/补跑轰炸客户

### 3. 需求 8：通知偏好扩展到客户公司维度

`notification_preferences` 从"只按用户"扩展为**用户 / 客户公司二选一**，
`_resolveChannel` 改为三级回退：

```
这个人自己的偏好 → 他所属客户公司的默认偏好 → 系统默认（仅站内信）
```

客户门户设置页新增「通知设置」卡片；`client_admin` 多一个「公司默认」页签。

### 4. 邮件模板机制

P0 的邮件队列对每封邮件都套同一个纯文本模板，渲染不出带按钮的报价邮件。
`notifications` 补 `email_template` + `email_payload`，队列按模板名分发；
认不出的模板名退回通用模板（老数据行为不变）。

---

## 修改文件清单

### 数据库

| 文件 | 操作 | 说明 |
|------|------|------|
| `server/database/migrations/108_quotation_email_notify.sql` | 新增 | 新表 `quotation_response_tokens`；notifications 补 email_template/email_payload 并放开 user_id；notification_preferences 加 client_id + 两个部分唯一索引 + 归属 CHECK；system_settings seed 6 个提醒配置 |

### 后端

| 文件 | 操作 | 说明 |
|------|------|------|
| `server/modules/quotation/service.js` | 新增 | 从 routes.js 抽出：状态常量、`createOrderFromQuotation`、`applyClientDecision`、`resolveActingUser`。门户和邮件链接两条路走同一段逻辑，避免行为漂移 |
| `server/modules/quotation/email.js` | 新增 | 收件人选取、链接组装、邮件入队 |
| `server/modules/quotation-response/service.js` | 新增 | token 签发 / 哈希 / 校验 / 单次占用 / 过期计算 |
| `server/modules/quotation-response/page.js` | 新增 | 免登录确认页与结果页的自包含 HTML |
| `server/modules/quotation-response/routes.js` | 新增 | 公开路由：GET 渲染确认页、POST 执行决策 |
| `server/modules/quotation/routes.js` | 修改 | send 端点改为"改状态 + 签发 token + 邮件入队"同一事务；accept/reject/pending 改为调用 service |
| `server/utils/email-service.js` | 修改 | 新增 `quotationEmail` / `paymentReminderEmail`；**新增 `esc()` HTML 转义并应用到所有模板**（含原有的 notificationEmail） |
| `server/utils/email-queue.js` | 修改 | 按 `email_template` 分发模板 |
| `server/utils/settings.js` | 新增 | system_settings 的类型化读取（`'false'` 直接当布尔用永远是 true，统一收口） |
| `server/utils/cron-jobs.js` | 修改 | 重写账单任务：自动标记逾期 + 到期前提醒 + 逾期催款，天数读配置 |
| `server/core/notification-engine.js` | 修改 | `_resolveChannel` 三级回退 |
| `server/modules/notification/routes.js` | 修改 | 偏好读写支持 `scope=USER/CLIENT` + client_admin 鉴权；**ON CONFLICT 补 WHERE 条件**（踩坑 020） |
| `server/modules/system/routes.js` | 修改 | `PUT /settings` **收紧为仅运营**；按影响行数如实反馈未知配置项 |
| `server/app.js` | 修改 | 挂载 `/api/v1/quotation-response` |

### 前端

| 文件 | 操作 | 说明 |
|------|------|------|
| `admin/src/pages/Settings.tsx` | 修改 | 新增「报价与账单提醒」卡片（6 项配置，写 system_settings），含天数范围校验 |
| `customer-portal/src/components/NotificationPreferences.tsx` | 新增 | 通知设置卡片，事件清单从后端拉取，client_admin 可切换公司默认 |
| `customer-portal/src/pages/Settings.tsx` | 修改 | 接入通知设置卡片 |
| `customer-portal/src/contexts/AuthContext.tsx` | 修改 | User 接口补 `roleCode`（登录接口一直有返回，只是没声明） |

---

## 关联影响分析

- **顺带修掉的越权**：`PUT /system/settings` 此前只挂 `authenticateToken`，
  任何登录账号（含客户门户、承运商门户）都能改信用检查、自动开票等全局开关。已收紧为仅运营。
- **P0 邮件队列不受破坏**：老通知行 `email_template` 为 NULL，队列继续走通用模板，行为完全不变。
- **`notifications.user_id` 放开为可空**：只为支持"纯对外邮件"（收件人不是系统用户）。
  存量行 user_id 都非空；按 user_id 查站内信的语句无需改动（NULL 行天然不匹配）；
  已加 CHECK 卡住"user_id 和 email_to 都为空"的无意义行。
- **order 模块只调用不修改**：`createOrder` / `updateStatus` 的签名与行为未动。
- **carrier-portal 不受影响**：承运商不参与报价流程。
- **确认过不受影响**：CMR / 清关 / 放单 / GPS 不读报价与通知偏好表。

---

## 本地验证结果

| 验证项 | 结果 |
|--------|------|
| 全新空库跑完 001→108 | ✅ 无错误 |
| 108 重复执行（幂等） | ✅ 无错误 |
| 公司级偏好唯一性 | ✅ 同公司同事件插第二条被 `uq_notification_pref_client` 拒绝 |
| 偏好归属 CHECK | ✅ user_id / client_id 都为空被拒绝 |
| 通知目标 CHECK | ✅ user_id / email_to 都为空被拒绝 |
| 部分索引 ON CONFLICT | ✅ upsert 两次得 1 行且值已更新 |
| 偏好三级回退 | ✅ 只有公司→用公司的；个人+公司→个人覆盖；都没有→回退默认 |
| token 强度与哈希 | ✅ 明文 43 字符、哈希 64 位十六进制、同明文哈希稳定、两次不重复 |
| token 单次使用 | ✅ 首次占用 true，并发再占用 **false**（防重复建单） |
| token 过期计算 | ✅ 已过期的 valid_until 回退默认天数；未来的落到当天 23:59 |
| 重发作废旧链接 | ✅ 旧链接变 USED，新链接有效 |
| **完整链路** | ✅ 发送报价 → 邮件入队（收件人取询价联系人）→ 渲染出三个按钮 → 取链接 token → 确认 → 报价 CONVERTED + 订单 PENDING_REVIEW |
| HTML 转义 | ✅ 客户名传 `<script>` 在报价邮件和通知邮件里都被转义 |
| 账单提醒 SQL | ✅ 日期区间查询语法有效 |
| Express 路由顺序 | ✅ quotation-response 2 条 / quotation 14 条 / notification 7 条均正确 |
| `npx tsc --noEmit` | ✅ admin 0 错误、customer-portal 0 错误 |
| `npm run build` | ✅ 两端均通过 |

---

## 上线记录（2026-08-01 23:53-23:58，已完成）

顺序不能反 —— 迁移必须先于部署，反了会让新代码查不存在的列。

1. ✅ 备份 RDS：`/var/backups/germany-box-db/germany_box_transport_20260801_235310.sql.gz`（38K，gzip 校验通过）
2. ✅ 生产执行 `108_quotation_email_notify.sql`，并复跑确认幂等
3. ✅ push `3741198..9e3c3da` 触发 CI/CD（run 30706941370，success）

`APP_BASE_URL` **未在生产 .env 配置**：不配会正确退回 `CORS_ORIGIN`
（当前 `https://47.83.241.117`），确认链接拼出来是对的，少改一处生产配置。
新域名启用时两者一起改，改完 `pm2 delete all` 再 start（踩坑 005）。

### 生产验证结果

| 验证项 | 结果 |
|--------|------|
| 迁移结构 | `quotation_response_tokens` 已建；notifications 补 2 列且 `user_id` 已放开为可空；两个部分唯一索引就位 |
| 配置 seed | 6 项提醒配置全部写入 |
| 迁移幂等 | 复跑无错误 |
| 新模块文件到位 | `modules/quotation-response/`（3 文件）、`modules/quotation/{email,service}.js`、`utils/settings.js` |
| 新 cron 生效 | 启动日志出现新文案「账单提醒+逾期标记(9:00)」 |
| `GET /system/settings` | 200，提醒项类型正确（布尔是布尔、数字是数字） |
| `GET /notifications/event-types` | 200，11 个事件 |
| `GET /notifications/preferences?scope=USER` | 200 |
| `GET /notifications/preferences?scope=CLIENT`（client_admin） | 200 |
| 免登录确认页（乱码 token） | **404**，未泄露任何信息 |
| **客户改系统配置** | **403**（修复前任何登录账号都能改），且库里 `credit_check_enabled` 未被改动 |
| 三端 + 官网页面 | 均 200 |
| 后端错误日志 | 无异常（仅上述 403 测试留下的「权限拒绝」记录，属预期） |

### ⚠️ 未在生产实跑的路径：发送报价 → 收邮件 → 点链接确认

两个原因：
1. 走一遍会真实生成 QUO 凭证，接受还会生成订单凭证 —— 已过账凭证按 ERP 规则
   只能冲销不能删除，不该为测试在账上留痕（与 P3 同一口径）。
2. 发信域名认证没配好，邮件根本送不到，这一步现在也验不出结果。

该链路已在本地对着真实 PostgreSQL 完整验证：入队 → 模板渲染出三个按钮 →
从链接取出 token → 校验 → 单次占用 → 报价 CONVERTED + 订单 PENDING_REVIEW。

**DNS 修好后，第一次发真实报价时请人工盯一遍**：收件箱能收到、
点链接先看到确认页（不是直接生效）、再点一次才建单、同一链接第二次点提示已使用。

---

## 遗留 / 后续

1. **⚠️ 邮件目前发出去也会被收件方丢弃 —— 上线前必须先修 DNS**

   2026-08-01 实测发信域名 `box-cargo.de` 的认证状态：

   | 记录 | 现状 |
   |------|------|
   | SPF（TXT `@`） | **不存在** |
   | DKIM（`strato._domainkey`） | **不存在** |
   | DMARC（TXT `_dmarc`） | `v=DMARC1;p=reject;` |

   正是踩坑 012 记的那个最糟组合：DMARC 要求最严格的拒收策略，
   但 SPF / DKIM 一个都没有 → DMARC 判定必定失败 → Gmail / iCloud
   **直接丢弃，连垃圾箱都不放**，而系统日志显示"发送成功"。

   生产 SMTP 本身是好的（`info@box-cargo.de` via smtp.strato.de，账号密码已配）。
   邮箱还在 Strato，DNS 也还指向本机，所以**不用等新域名，现在就能修**：

   ```
   TXT  @        v=spf1 include:_spf.strato.com -all
   TXT  _dmarc   v=DMARC1; p=none; rua=mailto:info@box-cargo.de
   DKIM          在 Strato 邮箱控制面板开启，按提示加它给的 DNS 记录
   ```

   先把 DMARC 降到 `p=none` 观察，确认 SPF/DKIM 都通过后再收紧回 `p=reject`。

   **在 DNS 修好之前，P4 的邮件功能等于停摆**：报价邮件会正常入队、
   `email_status` 会变 SENT，但客户收不到。
   不影响的部分：客户门户「我的报价」照常可以确认、站内通知照常、
   逾期自动标记照常、通知设置照常。

2. **邮件实际投递没在本地验证**：本地无 SMTP，只验证到"入队 + 模板渲染正确"。
3. **报价邮件目前只发一个收件人**（询价联系人 → 客户联系邮箱 → 开票邮箱，取第一个非空）。
   如果业务上需要抄送多人，要扩展成收件人列表。
4. **货物状态更新邮件**未单独接：状态变更已经走 `notificationEngine.notify`，
   客户把 `STATUS_UPDATE` 的邮件开关打开就会收到通用模板邮件。
   如果要更漂亮的专用模板，可复用已存在但一直没被调用的 `statusUpdateEmail`。
5. 单据流 amount 为 NULL 的老问题（踩坑 017）本次仍未动。
