# 2026-08-09 · 测试数据二次清理

**模块**：全局（订单 / 询价报价 / CMR / 客户 / 承运商 / 用户）
**需求来源**：飞书《EU-TMS 开发意见表》第 6 条（苏凌，2026-08-07 提，优先级高）

---

## 需求

| 项 | 内容 |
|---|---|
| 现状描述 | 系统里的订单询价、运输公司、客户等数据不是真实的 |
| 期望效果 | 删除询价单、订单、运输公司、报价单；客户删安百保留翼能；用户只留苏凌、小杜、小丁、管理员；CMR 数据也删 |

8-06 已做过一次正式运营初始化（`init-production-data.js`），这次是之后又积累的一批测试单据。

## 与原始清单的两处偏差（Frank 2026-08-09 决定）

1. **订单 `EU-20260808-0004` 保留**。苏凌 8-07 提需求时这条还不存在，是 8-08 新建的。
2. **它的上游 `INQ-20260807-0004` + `QUO-20260808-0003` 一并保留**。
   这条订单是从报价单转来的，删掉上游会让 `documents.source_doc_id` 和 `document_flow`
   变成悬空引用（两处都没有外键约束，删了不报错，只是查不到来源）。详见踩坑 061。

## 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `server/scripts/cleanup-test-data-20260809.js` | 新增 | 一次性清理脚本，检查模式 + `--fix`，数据库改动走单事务，OSS 删除放事务之后 |
| `docs/踩坑经验库/061_只保留一条单据却删掉它的上游会留下无人报错的悬空引用.md` | 新增 | 本次发现的坑 |
| `docs/开发记录/2026-08-09-测试数据二次清理.md` | 新增 | 本文件 |

**没有改任何业务代码**——纯数据清理，前后端一行未动。

## 执行结果（生产库，2026-08-09 23:18 CEST）

执行前手工备份：`/var/backups/germany-box-db/germany_box_transport_20260809_231745.sql.gz`
（59K，已验证 gzip 完整、含 68 张表数据、含本次待删记录）

删除合计 114 行：

| 对象 | 行数 |
|------|------|
| 订单 EU-20260806-0001 / 0002、EU-20260807-0003 | 3 |
| 订单状态日志（随订单 CASCADE） | 14 |
| 订单附件、CMR | 1 / 1 |
| 报价单 2 张、询价单 6 张 | 8 |
| ERP 凭证 / 单据流 | 12 / 2 |
| 变更追踪 / 通知 / 代客登录票据 | 39 / 25 / 3 |
| 承运商 SGF、Eurosped | 2 |
| 客户 安百 | 1 |
| 用户 Kiu、speedtrans、siemens | 3 |
| OSS 文件（均属 EU-20260806-0002） | 2 |

保留：订单 `EU-20260808-0004` + 上游报价询价 + 3 张凭证 + 单据流 1 条；
客户翼能；用户 admin / suling / duguirong / xiaoding；全部系统配置。

编号计数器：`CMR 1→0`、`CAR 2→0`（对应数据清空）；
`ORD=4`、`INQ=7`、`QUO=3`、`CLT=2` 保持不动——都还有保留记录，归零会跟现存编号重号。

## 核对

脚本自带 9 项核对全部通过，另做独立复核：

- `document_flow` 悬空引用 = **0**，`documents.source_doc_id` 悬空引用 = **0**（链路完整）
- OSS bucket 剩余对象 = **0**
- pm2 两个实例 online，首页 / 登录页 / `/api/health` 全 200

## 关联影响

1. **承运商现在是 0 家**。派单、承运商询价邮件都用不了，需要先在运输公司页建真实承运商。
   `CAR` 计数器已归零，新建的第一家是 `S-00001`。
2. **承运商门户暂时无可登录账号**（`speedtrans` 已删，且没有承运商实体可绑）。
   客户门户不受影响：`suling`、`duguirong` 都绑在翼能上，正常。
3. **变更追踪删掉 39 条**，保留订单那 4 条完整留着。这批全是测试期审计记录，
   ERP 审计日志删除不可逆，已在执行前向 Frank 说明。
4. **确认不受影响**：权限 76 条、角色 8 个、科目表、编号范围定义、定价配置、
   过账期间、`md_*` 主数据字典、发票模板、系统设置——全部未动。
5. 前端三端一行未改，**不需要重新构建部署**。

## 注意

脚本是一次性的，带日期后缀，不要重复执行——白名单里的编号一旦对不上会直接中止，
不会误删，但也没有再跑的意义。
