# 🔻 交接：下次开发从这里开始

> 最后更新：2026-08-04（收尾轮 + 数据层验证轮）
> 上一轮做完的范围：**需求表 1.0 收尾项——三端 ESLint 配置 + 门户绑定校验**
> 再上一轮：P9 三语国际化全部完成 + 5 个既有 bug 清理
> 新开对话时，**先读这一篇**，再按需要跳到下面链接的详细记录。

---

## 零、最近一轮（收尾）做了什么

详见 `docs/开发记录/2026-08-04-收尾项-ESLint配置与门户绑定校验.md`

1. **三端 ESLint 配置补齐**——原来以为只有 carrier-portal 坏，实际
   **三端都缺 `.eslintrc.cjs`**，`npm run lint` 从建项目起就没跑起来过。
   补配置 + 清掉 25 处死代码，现在三端 lint 退出码 0。
   （35 个 `react-hooks/exhaustive-deps` 警告保留为提示，未动）
2. **登录时校验门户账号绑定的公司是否存在**——补上踩坑 035 说过但
   从没实现、也无处实现的防护（`clients`/`carriers` 根本没有删除路由）。
   现在绑定悬空会拒绝登录并返回 `BOUND_COMPANY_MISSING`，
   不再"登得进去但满屏空白"。
3. 新踩坑 **038**：声称存在的防护与校验，其实从没生效过。
   同时订正了踩坑 035 那条不成立的防护规则 #3。

> ⚠️ 数据库本轮无改动，迁移编号仍停在 120，下次从 **121** 起。

---

## 一、当前状态一句话

**P9 三语国际化（中/英/德）开发侧 100% 完成并全部上线。**
三端前端 + 后端消息 + 邮件 + Excel + 通知，全链路三语。
顺带清掉了 P9 期间发现的 5 个既有 bug。

此后又做了收尾轮（ESLint + 门户绑定校验）和数据层验证轮，见第零节。
CI 绿，生产已部署。

> 别在这里记「最新提交是 xxx」——每轮都会过期反而误导，
> 要看当前状态直接 `git log --oneline -5`。

---

## 二、⚠️ 唯一还在等的事（不在开发这边）

**德语同事校对术语表** → `docs/i18n术语表.md`

里面有四部分，每行都留了「校对后」空列：

| 部分 | 条数 | 校对后改哪里 |
|---|---|---|
| 承运商门户 / 客户门户 / 运营管理端界面文案 | ~2400 | 各端 `src/i18n/locales/de.json` |
| 基础数据（国家/港口/币种…）德语名 | 51 | **数据库** `md_*` 表的 `name_de` 列 |
| 后端 Excel 表头 + 邮件模板 | 103 | `server/i18n/locales/de.json` |
| 通知标题/正文 | 24 | 同上 |

**特别标了要确认的两条**：`EMC-内电` / `EMC-内电保` 是公司内部叫法，
我按字面译成 `EMC-Elektro` / `EMC-Elektro (Zolllager)`，见迁移 117 的注释。

> 校对结果回来后，跟新对话说「德语校对回来了」+ 把表给我，我批量回改。
> 语言包改完记得跑 `npm run check:i18n`。

---

## 三、三处待确认 —— **数据层已于 2026-08-04 全部验过，只剩 UI 层**

> 原文写的是「三处我验不了」。后来发现其中大部分**不用登录也能验**：
> 这几个 bug 都属于「字段名 / 枚举值对不上」，可以拿代码直接对生产库核，
> 再把接口的真实 SQL 在事务里跑一遍。已按这个办法验完，结论如下。
> **别再重复验数据层**，剩下的只有肉眼看渲染这一层。

1. **派单页**（`/orders/:id/assign`）— ✅ SQL 正确性已验，但**样本数据已作废**
   当时跑接口真实 SQL，结果与预期逐项吻合（SGF `100.0%（1 单）`、
   Eurosped `0 样本`、覆盖路线 `lunen → madrid`）。

   > ⚠️ **2026-08-06 生产测试数据已被清空**（orders / quotations / inquiries 全为 0），
   > 上面这些具体数值**已不存在，别再照着找**。
   > 仍然成立的是「SQL 与字段名正确」这个结论；作废的只是样本值。
   > 现在所有承运商都会显示 `-`（0 样本），**这是正确行为，不是 bug**。

   **剩余**：等有真实订单后再看页面渲染。

2. **承运商门户「公司设置」**— ✅ 保存路径已端到端验过（数据层）
   在事务里跑了 `PUT /system/settings/company` 的真实 UPDATE，
   再用 `GET` 的真实 SELECT 读回来：**8 个字段全部存住**
   （含迁移 119 新加的 `bank_name` / `bank_account`），然后 ROLLBACK，
   生产数据未变。当初「填完保存后数据丢失」的要害位置已证明是好的。
   另核对：`clients` 7 列 + `carriers` 8 列白名单**全部存在**，无对不上的列。
   **剩余**：`speedtrans` 启用后在页面上填一次看 UI 层（⚠️ 启用前必须先改密码，见第六节）。

   > 2026-08-05 另一个对话给 **PUT**（不含 GET）挂上了权限码并加了迁移 121——
   > 此前任何绑定了公司的门户账号都能改承运商银行账号，是资金欺诈面。
   > 已核实生产：迁移 121 已生效，`speedtrans` 是 `carrier_admin`、
   > `carrier_portal:company_settings` 已授权给该角色，**启用后不会 403**。
   > 我验的是 SQL 落库路径（仍成立），他们补的是前面那道权限闸，两者不冲突。

3. **CMR 管理的筛选 Tab** — ✅ 取值域已证明闭合
   `sign_status` 的可能取值只有 4 个（INSERT 默认 `UNSIGNED`；
   `PUT /:id/sign-status` 白名单 `UNSIGNED/SENDER_SIGNED/RECEIVER_SIGNED/COMPLETED`，
   非法值 400）。前端两个 Tab 正好覆盖这 4 个，**无遗漏无多余**。
   「有异常」Tab 走 `hasDamage=true` → `has_damage_note = true`（boolean 列已确认存在），
   不再往 `signStatus` 里塞非签署状态值（那正是踩坑 036 的原病因）。
   三条筛选 SQL 都在生产真跑过、执行成功（含从没跑过的多值 `= ANY()` 路径）。
   **剩余**：等真有 CMR 数据后点一遍，确认能筛出东西（现在 0 条，筛出 0 行是正确行为）。

---

## 四、上一轮改了什么（按主题，附详细记录链接）

### P9 三语国际化

| 范围 | key 数 | 详细记录 |
|---|---|---|
| 运营管理端 | 1817 | `docs/开发记录/2026-08-03-P9三语国际化.md` |
| 客户门户 | 572 | 同上 |
| 承运商门户 | 399 | 同上 |
| 后端（Excel/邮件/通知） | 127 | 同上 |
| 基础数据德语名 | 生产 51 行全满 | 同上 |

关键设计（新对话要改 i18n 前必读）：

- **前端**：`react-i18next`，每端一份 `src/i18n/locales/{zh,en,de}.json`
- **后端 API 提示语**：**不在后端翻译**。响应里 additive 带 `messageCode`
  （`server/middleware/messageCode.js` 按文案查 `server/utils/message-codes.js` 补码），
  前端 `api.ts` 按 `apiMessage.<CODE>` 查语言包，查不到退回后端原文
- **后端自己渲染的东西**（Excel 表头、邮件、通知）：走 `server/i18n/locales/`，
  用 `t(lang, key, vars)`；语言从 `Accept-Language` → `users.language` → zh
- **校验**：`npm run check:i18n` 一次查四个包（三前端 + 后端）

### 顺带清掉的既有 bug

| 问题 | 记录 |
|---|---|
| CMR 两个筛选 Tab 查不到数据 + 仪表板 5 处假数据 | `docs/开发记录/2026-08-04-CMR筛选与仪表板假数据修复.md` |
| 门户「公司设置」保存后数据丢失 | `docs/开发记录/2026-08-04-门户公司设置保存丢失修复.md` |
| 派单页准时率/覆盖路线接口根本不返回 + 中文当值改 code | `docs/开发记录/2026-08-04-派单页真实数据与中文当值改code.md` |

P9 期间新立的踩坑：**036**（筛选 Tab 用小写 key）、**037**（编辑弹窗回填错字段会改写数据）。

---

## 五、数据库：迁移编号用到 121，下次从 **122** 起

> ⚠️ **121 已被另一个对话用掉**（`121_company_settings_permission.sql`，
> 给 PUT 公司资料补权限码）。**开工前一律 `ls server/database/migrations/`
> 看最大号 +1，不要照抄本文档写的数字**——本仓库经常有多个对话并行。

| 编号 | 内容 | 是否动存量 |
|---|---|---|
| 116 | 7 张 `md_*` 表加 `name_de` + 回填种子 74 行 | 加列 + 回填 |
| 117 | 补运营自己加的 16 行德语名 | 只 UPDATE 空值 |
| 118 | `notifications` 加 `title_key`/`message_key`/`payload` | 纯加列 |
| 119 | `carriers` 加 `bank_name`/`bank_account` | 纯加列 |
| 120 | 特殊要求中文值 → code | **UPDATE 28 行订单** |

每次都是：**pg_dump 备份 → 跑迁移 → 验证 → 才 push**。
120 因为动存量数据，额外先做了「事务内执行后 ROLLBACK」的干跑。

> ⚠️ **部署顺序有硬依赖**：凡是代码 SELECT/UPDATE 了新列的迁移，
> 必须**先跑迁移再让 CI 部署代码**，反了会 500。116/118/119 都踩这条。

备份都在服务器 `/root/backups/`，最近几个：
`germany_box_transport_20260804_011910.sql.gz`（迁移 120 前）。

---

## 六、已知但**没做**的事（要做得先跟 Frank 确认）

1. **通知走通用模板那类，站内信标题正文已三语，但老数据仍是中文**
   —— 这是设计如此：迁移 118 是 additive 的，只有带 `title_key` 的新通知能翻译，
   9 条存量老通知继续显示中文。不是 bug。

2. ~~**`carrier-portal` 的 `npm run lint` 跑不起来**~~
   **已解决（2026-08-04 收尾轮）**：实际是三端都缺配置，已全部补上。

3. ~~**踩坑 035 的两个悬空绑定**~~ **已改绑（2026-08-04）**
   `siemens` → 安百、`speedtrans` → SGF，生产悬空绑定数已归零。
   体检脚本：`cd server && node scripts/check-dangling-entity-bindings.js`
   （退出码 0 = 干净）。详见 `docs/开发记录/2026-08-04-悬空绑定改绑与体检脚本.md`。

   🔒 **两个账号仍保持停用**。密码轮换状态（2026-08-06 订正，此前误写"无证据表明已轮换"）：
   nginx 日志证实 Frank 已于 **08-03 09:22 在用户管理页轮换过 `admin` / `siemens` / `speedtrans`**
   （`admin123` 实测已不匹配当前哈希）；`admin` 又于 **08-06 下午**经
   `server/scripts/reset-user-password.js` 重设（08-03 那次设的密码遗失了）。
   旧明文密码在 git 历史中仍可读（`60778fd` 只删了当前文件）。
   启用 siemens/speedtrans 前建议还是用该脚本再重设一次——页面重置只强制 ≥6 位，
   脚本强制 ≥12 位且三类字符，顺便确保当前值不是任何公开旧值。

4. ~~**生产有 26 单在途且已超过预计到达日**~~ **已随清库消失（2026-08-06）**
   那 26 单是测试数据，`e1ce197` 已全部清除。等真实业务单进来后重新观察。

5. **准时率口径：暂不改，但判断依据已随清库作废**（2026-08-06 订正）
   现用 `orders.delivery_date`。2026-08-04 的结论建立在「29 单里只有 1 单填了
   `delivery_date`、0 单填了 `expected_delivery_date`」之上，
   而 **08-06 生产测试数据已清空，这些统计不再成立**。
   口径之争要等真实业务单积累起来、看清运营到底填不填这两个日期，才谈得上。
   在那之前不动代码。

6. **35 个 `react-hooks/exhaustive-deps` 警告**（2026-08-04 补 ESLint 后暴露）
   补依赖数组可能引发无限重渲染，每个都要逐页验证，建议单开任务，别顺手改。

---

## 七、新对话开工检查清单

```bash
# 1. 拉最新（仓库里可能还有别的对话在推进）
git fetch origin && git rebase origin/main

# 2. 要改代码就先开 worktree（全局 CLAUDE.md 规定）
#    2026-08-04 已把历史遗留的 14 个 worktree 和 17 个已合并分支全清了，
#    现在应该只有 main。用完自己那个记得也清掉——
#    留着会让下个对话误读（本交接文档一度只存在于某个废弃 worktree 里）

# 3. 装依赖（worktree 是干净的）
npm install

# 4. 动 i18n 前先跑一次基线
npm run check:i18n

# 5. 提交前
cd admin && npm run build:check     # 三端各跑一次，build 不做类型检查（踩坑 014）
npm run check:i18n
```

**必读的踩坑**（按和日常开发的相关度排）：
- **033** 前端字段名凭想象写 → 写任何列表页前，先把接口 SQL 拉出来核对列名
- **036** 筛选 Tab 的 key 用小写 → 凡是拼进查询的常量，必须能在迁移文件里搜到
- **037** 编辑弹窗回填读错字段 → 「读出来→回填→存回去」的闭环字段名要核两遍
- **004** 状态枚举大小写、**002** NUMERIC 返回字符串、**014** build 不做类型检查
- **015** rebase 无冲突但语义回退主线修复 → push 前必看 `git diff origin/main..HEAD`
