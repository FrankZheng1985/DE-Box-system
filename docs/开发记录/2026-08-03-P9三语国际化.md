# P9 · 三语国际化（中 / 英 / 德）— ✅ 已完成

> 开始日期：2026-08-03
> 计划出处：`docs/开发计划/2026-08-01-需求表1.0开发计划.md` 第 P9 节（需求 10）
> 本文件是跨会话的"活文档"，每次开工先读它
> 进度：**三端前端 + 后端全部完成并上线**（5 次对话，2026-08-03 ~ 08-04）
> 只剩德语同事校对术语表后批量回改

## 🎯 目标

三端（运营管理端 / 客户门户 / 承运商门户）+ 后端消息全部支持 **中文 / 英文 / 德文** 切换，
语言选择持久化到 `users.language`（字段早就有，一直没人读写）。

## 📊 摸底数据（2026-08-03 实测）

| 端 | 文件数 | 含中文文件 | 中文字符量 | 状态 |
|---|---|---|---|---|
| admin 运营端 | 63 | 62 | ~24000 | ✅ 已完成（第 3、4 次对话） |
| customer-portal | 22 | 20 | ~3200 | ✅ 已完成（第 2 次对话） |
| carrier-portal | 13 | 12 | ~1040 | ✅ 已完成（第 1 次对话） |
| 后端 message / 邮件模板 / Excel 表头 | — | — | — | ✅ 已完成（第 5 次对话） |

三端原本**零 i18n 基础设施**（`useTranslation` 全仓库零命中）。
另有 43 处硬编码 `toLocaleString('zh-CN')` 之类要顺带收敛到 `Intl`。

## ✅ 已完成 / ⏳ 待办

### 第 1 次对话（2026-08-03）：基建 + 承运商门户样板
- [ ] carrier-portal 装 `i18next` + `react-i18next`
- [x] `src/i18n/` 基础设施（初始化 + 语言检测 + 持久化）
- [x] `src/i18n/locales/{zh,en,de}.json` 语言包（152 个 key × 3 语言，已校验完全对齐）
- [x] `LanguageSwitcher` 语言切换组件（Layout 侧边栏 + 登录页各一处）
- [x] `src/utils/format.ts` 用 `Intl` 统一日期/金额格式化
- [x] 12 个页面/组件文案全部抽成 key（复扫零残留）
- [x] 后端登录接口返回 `language` 字段
- [x] 补 `carrier-portal/src/vite-env.d.ts`（缺失导致 `tsc` 报 `import.meta.env` 无类型）
- [x] 修 3 个页面「靠文案判断成败」的提示条（踩坑 032）
- [x] 导出 `docs/i18n术语表.md` 给德语同事校对
- [x] `npm run build:check` 类型检查 + 构建通过
- [x] 提交 + 部署 + 生产验证（第一轮，commit `0ae1ec7`）

### 第 1 次对话·补丁轮（Frank 看生产页面发现整列空白）
- [x] 修 `TransportTasks` / `Billing` 的字段错配（踩坑 033）
- [x] 修「待结算金额」恒为 0（枚举值 `PENDING`/`APPROVED` 数据库根本不存在）
- [x] 重量按 `cargo_weight_kg` 正确显示 kg（原来写死 `t`）
- [x] 订单状态补齐到 13 个、付款状态改成真实的 5 个、新增 `businessType` 三语
- [x] 德语徽章换行修复（`whitespace-nowrap`）+ 两张表列宽按德语重排
- [x] 重新部署 + 生产验证（commit `c985595`）

### 第 1 次对话·管理端连带修复（Frank 截图逐页发现）
- [x] `StatusBadge` 补 7 个缺失状态：`ACTIVE`/`INACTIVE`/`UNSIGNED`/
      `SENDER_SIGNED`/`RECEIVER_SIGNED`/`PENDING`，并让被忽略的 `type` prop 真正生效
- [x] `CarrierList` 评分字段 `rating` → `performance_score`（恒显示 0.0）
- [x] 四张统计卡片字段错配：CMR `signed`→`completed`、
      清关 `exception`→`exceptions`、放单 `pending_mail`/`mailed`→
      `original_pending`/`original_sent`
- [x] 提交 `336bd4d` + `ee7f4b2`，CI 自动部署成功

### 第 1 次对话·CI 修复（部署管线连红三次）
- [x] 根 `workspaces` 漏了 `carrier-portal`，CI 装不到 `i18next` → 构建失败
- [x] CI 增加 `cd carrier-portal && npm ci`；根 `install:all` 补上 carrier-portal
- [x] 立档踩坑 034，提交 `bf067b4`，CI 恢复绿色

### 第 2 次对话（2026-08-03）：customer-portal ✅
- [x] 按 workspace 方式装 i18next（客户门户在根 workspaces 里，与承运商门户不同）
- [x] 基建照抄承运商门户：i18n/index.ts、utils/format.ts、LanguageSwitcher
- [x] 三语包各 336 个 key，13 组状态枚举全部进语言包
- [x] 20 个文件文案全部抽 key，复扫零残留
- [x] 通知事件名、角色名改为前端语言包 + 后端值兜底
- [x] **翻译前先核对了 9 个页面的接口字段**（踩坑 033 纪律），修掉 5 个页面的错配
- [x] 清空 node_modules 按 CI 顺序完整复现，三端构建通过
- [x] 术语表追加客户门户 336 条

### 第 3 次对话（2026-08-03）：admin 运营端 —— **上半段：共享层** ✅
- [x] 按 workspace 装 i18next（锁文件只增 2 行，i18next 已被客户门户装过）
- [x] 基建三件套 + 三语包 176 个 key
- [x] `StatusBadge` 41 个状态文案搬进语言包 + 6 个小写遗留值加别名
- [x] `Sidebar` 15 个菜单、`Header` 路由标题映射 → 语言包 key
- [x] `Header` 右上角加语言切换器
- [x] `Login` / `ConfirmDialog` / `AuthContext` / `api.ts` 全部抽 key
- [x] 顺带修：`/settings/open-api` 在路由标题映射里排在 `/settings` 后面，
      前缀匹配先命中 `/settings`，开放 API 页标题一直显示成「系统设置」
- [x] 提交 `cdeac82`

**刻意保留中文**：`main.tsx` 两处崩溃兜底屏（要在 i18n 自己挂掉时仍能显示）、
`console.error`、`useAuth` 的开发者错误提示。

### 第 4 次对话（2026-08-03）：admin 运营端 —— **下半段：56 个页面/面板** ✅

分 5 次提交推送，每次都单独跑 `build:check` + rebase 后重新验证再 push：

| 提交 | 范围 | 文件数 |
|---|---|---|
| `4039d77` | 常量层 + 订单域 + 询价报价域 + 仪表板 | 24 |
| `43fe244` | 主数据域（客户 / 承运商 / 基础数据） | 11 |
| `853d111` | 单据物流域（CMR / 清关 / 放单 / GPS） | 7 |
| `6254de5` | 财务域（应收应付 / 发票模板 / 会计三页） | 9 |
| `04468e8` | 系统域 + 开放 API（含 P8 新拆的三个面板） | 11 |

- [x] 常量层改成只存值和语言包 key：`businessTypes` / `inquiryQuotation` /
      `carrierInquiry` 的 `*_LABELS` 换成 `*LabelKey()`，Tab 的 `label` 改 `labelKey`
- [x] `getStatusLabel()` 改为接收 `t`，切语言时组件会跟着重渲染
- [x] MasterData 这种配置驱动页，`TAB_CONFIGS` 整体改 `labelKey` / `placeholderKey`
- [x] 三份语言包各 **1578 个 key**，key 集合与插值占位符全部对齐
- [x] 把校验脚本固化成 `scripts/check-i18n-keys.js`（`npm run check:i18n`，三端一起查）
- [x] 术语表追加 admin 的 1495 条，供德语同事校对

**刻意保留中文的四处**（复扫确认，不是漏翻）：
1. `main.tsx` 两处崩溃兜底屏 —— 要在 i18n 自己挂掉时仍能显示
2. `useAuth` 的开发者错误提示 —— 面向开发者不面向用户
3. **直接入库的枚举值**：`orders.special_requirements`、
   `customs_documents.file_type`（库里存的就是中文，翻了会写坏数据，见下方"遗留问题"）
4. `LanguageSwitcher` 里的「中文」—— 语言名按惯例用该语言自己的写法

### 第 4 次对话·顺带修掉的既有 bug

按踩坑 033 纪律「翻译前先拉接口 SQL 核对列名」，这一批揪出 12 处：

| 页面 | 问题 | 现象 |
|---|---|---|
| Dashboard | 柱状图 `statusDistribution` 用小写 key 匹配大写枚举 | 标签一直显示原始英文 |
| Dashboard | `count` 是 BIGINT 回字符串，没转数字就算高度 | 侥幸没错，已显式转 |
| CarrierDetail | `contact_person`/`phone`/`email` → 表里是 `contact_name`/`contact_phone`/`contact_email` | 联系人三行一直空白 |
| CarrierDetail | 评分读 `rating` → 表里是 `performance_score` | 评分卡片和星标恒 0 |
| ClientList / CarrierList | interface 同样写了不存在的列名（未渲染） | 无可见影响，一并改对 |
| CMRManagement | `order_no` / `route` / `has_damage` / `upload_time` 四个字段全错 | 订单号、路线、上传时间三列空白 |
| CustomsManagement | `order_no` / `clearance_status` / `broker_name` / `document_count` 全错 | 订单号、报关行空白，状态徽章空，文件数恒 0 |
| **CustomsManagement** | **「更新状态」弹窗回填也读错字段** | **点开保存一次就把真实状态改回 PENDING（踩坑 037）** |
| ShippingRelease | `order_no` → `order_number` | 订单号列空白 |
| 清关 / 放单 / CMR | 状态筛选 Tab 的 key 是小写，后端和大写枚举等值比较 | **每个筛选 Tab 点下去都是空的（踩坑 036）** |
| InquiryEdit / QuotationCreate / InvoiceTemplates | 循环变量命名成 `t`，遮住翻译函数 | 翻译后才会暴露，提前改掉 |
| 各页 | 写死 `toLocaleString('zh-CN'/'de-DE')` | 收敛到 `utils/format` 的 Intl 版本 |

新立档：`docs/踩坑经验库/036`（筛选 Tab 小写）、`037`（回填错字段会改写数据）。

### 第 5 次对话（2026-08-03）：后端 —— **1/3 基础数据德语名** ✅

修掉了「前端翻完之后界面上唯一剩下的中文来源」：国家/币种/箱型/船司/港口/
车型/特殊要求这些下拉框，文案原来是后端用 `name_zh` 拼的。

- [x] 迁移 **116**：7 张 `md_*` 表加 `name_de`（可空）+ 回填种子 74 行
- [x] 迁移 **117**：补运营后来自己加的 16 行（EMC 各条服务线、武汉/海防/
      布达佩斯港、危险品与电池类特殊要求）——116 按 code 匹配，这些匹配不上
- [x] `server/utils/i18n.js`：`resolveLang()`（Accept-Language 按 q 值排序 →
      `users.language` → zh）+ `pickName()`（目标语言 → 英文 → 中文 逐级回退）
- [x] `/system/master-data/:type/options` 按请求语言拼 label
- [x] master-data 增删改查支持 `name_de`（列表可搜、表单可录）
- [x] 三端 `api.ts` 统一带 `Accept-Language` 头
- [x] MasterData 页 7 个类型的表单各加「德文名称」字段
- [x] 提交 `30b3ae6` + `9bbbbee`，CI 绿，生产实测通过

**关键约束（代码里已注释）**：`value` 一律保持原样、不跟着语言变。
它要和业务表里已存的值对得上（`orders.country` 存 `name_en`、
`special_requirements` 存 `name_zh`）。跟着语言变的话，德语界面建的单会存进
德语值，中文界面再打开就匹配不上了。

**生产验证（2026-08-03 23:45-23:52）**：
- [x] RDS 备份 `germany_box_transport_20260803_234545.sql.gz`（56K）
- [x] 迁移 116 → 7 张表加列成功，回填 36 行（生产行数与种子不同，运营改过）
- [x] 迁移 117 → 补齐 15 行，**7 张表 51 行德语名全满，零缺失**
- [x] 服务器上实跑 `pickName` / `resolveLang`：
      `de → Deutschland / Wuhan / Temperaturgeführt`；
      头解析 `de-DE,de;q=0.9,en;q=0.8→de`、`fr→zh`（认不出的回退中文）✅

> ⚠️ 部署顺序有硬依赖：**必须先跑迁移再部署代码**。反了的话 options 接口
> 查不到 `name_de` 列直接 500，所有下拉框失效。本次是先备份 → 跑迁移 →
> 验证 → 才 push 的。

### 第 5 次对话·续（2026-08-03/04）：后端 2/3 + 3/3 ✅ —— **P9 全部完成**

#### 2/3 API 消息码（提交 `9738f5a` + 补丁 `fc9357f`）

全仓库 300 处 `res.json({ message: '中文' })`，去重 229 条。
**没有逐个改调用点**，改成中间件按文案查表补码：

- `server/utils/message-codes.js`：229 条中文 → 222 个语义码
- `server/middleware/messageCode.js`：包一层 `res.json`，有中文 message
  且没 messageCode 时补一个；已显式写码的（auth/login）不覆盖
- 三端 `api.ts`：`translateByCode()` 按 `apiMessage.<CODE>` 查语言包，
  查不到退回后端 message；**成功响应的 message 也就地翻译**，
  页面继续用 `res.message` 就是当前语言，页面代码一行没改
- 三端语言包各加 227 条 `apiMessage.*`

选中间件而不是改 300 处的三个理由：完全 additive（老前端和接开放 API 的
合作方拿到的响应一个字没变）、业务代码零改动（不会手改出语法错误）、
以后新增消息只要往码表加一条就自动生效。

> **补丁轮**：生产实测发现认证/限流中间件走的是 `{ errCode, msg }` 老格式，
> 第一版只查 `message` 漏了 5 条。已改成两个字段名都查。
> 生产复测三种响应形态全部带码 ✅

#### 3/3 Excel 表头 + 邮件模板（提交 `f2ad540`）

- `server/i18n/locales/{zh,en,de}.json`：`excel.*` 69 条 + `email.*` 34 条
- `utils/i18n.js` 加 `t(lang, key, vars)`：目标语言 → 中文 → key 逐级回退
- **Excel**：订单/客户/承运商/询价/应收/应付六个导出、86 处表头按
  `Accept-Language` 渲染。已逐个核对 86 处 `t(lang,...)` 都在声明了 `lang`
  的那个路由作用域内
- **邮件**：3 个在用的模板（外壳 / 报价确认 / 账单逾期提醒）全部三语。
  发信是后台任务没有请求上下文，语言按收件邮箱查 `users.language`，
  查不到（客户联系人可能没有门户账号）就用中文；查语言失败也不挡发信
- `orderConfirmationEmail` / `statusUpdateEmail` 没有任何调用点（死代码），未动
- `scripts/check-i18n-keys.js` 扩到第四个包（server），
  `npm run check:i18n` 一次校验三个前端 + 后端

**生产验证（2026-08-04）**：服务器上实跑三语渲染 —— Excel 表头
`订单号 / Order no. / Auftragsnr.`、邮件主题
`报价单 QT1 请确认 / Please confirm quotation QT1 / Bitte Angebot QT1 bestätigen` ✅

## 🎉 P9 完成状态

| 范围 | 状态 | key 数 |
|---|---|---|
| 承运商门户 | ✅ | 394 |
| 客户门户 | ✅ | 567 |
| 运营管理端 | ✅ | 1807 |
| 后端（Excel + 邮件） | ✅ | 103 |
| 基础数据德语名 | ✅ | 生产 51 行全满 |

`npm run check:i18n` 四个包全绿。

### 后续对话
- [ ] 德语同事校对完术语表后，批量回改语言包
- [ ] （可选）通知邮件的标题/正文仍是中文，见下方遗留问题第 5 条
- [ ] ~~后端 message / 邮件模板 / Excel 表头~~ ✅ 已完成
      （按 `Accept-Language` 或 `users.language` 返回，样板见
      `server/modules/auth/routes.js` 的 login：响应里 additive 地带 `messageCode`）
      （master-data options 那条已在本次做完，见上）
- [ ] 德语同事校对完术语表后，批量回改语言包

## 🧠 关键决策

| 决策 | 结论 | 原因 |
|---|---|---|
| i18n 方案 | **react-i18next**（2026-08-03 Frank 拍板） | 行业标准，插值/复数/命名空间开箱即用；自研 t() 后期要自己补轮子 |
| 语言包放哪 | **每端各一份** `src/i18n/locales/`，不搞 monorepo 共享包 | 三端是三个独立 Vite 应用，为共享几十个通用词建 workspace 属于过度工程化 |
| 德语上线策略 | **AI 初稿直接上线**，同时导出术语表给德语同事校对，校对完批量回改（Frank 拍板） | 不阻塞上线；物流术语（CMR/清关/放单/LDM）人工兜底 |
| 语言优先级 | localStorage → `users.language` → 浏览器语言 → `zh` | 未登录也能记住选择；登录后以账号设置为准 |
| 持久化接口 | 复用 `PUT /api/v1/system/settings/account` 的 `language` 字段 | 该接口只挂 `authenticateToken`，三端账号都能调，不用新建端点 |
| 做的顺序 | carrier-portal（最小）→ customer-portal → admin | 最小端当样板跑通全链路，后两端照抄，返工风险最低 |

## 📁 涉及文件（第 1 次对话）

### 新增

| 文件 | 说明 |
|---|---|
| `carrier-portal/src/i18n/index.ts` | i18next 初始化、语言检测、`applyLanguage()` |
| `carrier-portal/src/i18n/locales/zh.json` | 中文语言包（152 key） |
| `carrier-portal/src/i18n/locales/en.json` | 英文语言包 |
| `carrier-portal/src/i18n/locales/de.json` | 德文语言包（AI 初稿，待校对） |
| `carrier-portal/src/components/LanguageSwitcher.tsx` | 语言下拉，已登录时写回 `users.language` |
| `carrier-portal/src/utils/format.ts` | `Intl` 金额/日期/数字格式化 |
| `carrier-portal/src/vite-env.d.ts` | 补 Vite 类型引用（原来缺，`tsc` 报错） |
| `docs/i18n术语表.md` | 三语对照表，给德语同事校对 |
| `docs/踩坑经验库/032_靠文案判断成败的提示条在多语言下必然失效.md` | 本次踩坑 |

### 修改

| 文件 | 改了什么 |
|---|---|
| `carrier-portal/package.json` | 新增 `i18next` + `react-i18next` 两个依赖 |
| `carrier-portal/src/main.tsx` | `import './i18n'` 启动时初始化 |
| `carrier-portal/src/contexts/AuthContext.tsx` | `User` 加 `language` 字段；登录/恢复会话时应用账号语言 |
| `carrier-portal/src/utils/api.ts` | 3 条错误提示改走 `i18n.t()` |
| `carrier-portal/src/components/Layout.tsx` | 菜单 label 改 labelKey；侧边栏加语言切换器；补 aria-label |
| `carrier-portal/src/pages/Login.tsx` | 全部文案抽 key；未登录也能切语言 |
| `carrier-portal/src/pages/Dashboard.tsx` | 文案抽 key；收入卡片改 `formatMoney` |
| `carrier-portal/src/pages/TransportTasks.tsx` | tab / 表头 / 按钮抽 key；状态 map 拆成「样式 map + 语言包文案」 |
| `carrier-portal/src/pages/UploadCMR.tsx` | 文案抽 key；签署状态走语言包；**提示条改 `{text, ok}`** |
| `carrier-portal/src/pages/GPSReport.tsx` | 文案抽 key；状态下拉走语言包；**提示条改 `{text, ok}`** |
| `carrier-portal/src/pages/Billing.tsx` | 文案抽 key；金额/日期改 Intl；**修 NUMERIC 字符串相加的求和 bug** |
| `carrier-portal/src/pages/CompanySettings.tsx` | 文案抽 key；**提示条改 `{text, ok}`**；只读取本表单认识的字段 |
| `server/modules/auth/routes.js` | 登录 SELECT 加 `u.language`，响应 user 里返回 `language` |

## 🔗 关联影响

1. **后端 `auth/routes.js` 是三端共用的登录接口**——多返回一个 `language` 字段是**纯新增**，
   客户门户和管理端的现有代码不读这个字段，不受影响；等它们做 i18n 时直接就能用。
2. **`PUT /api/v1/system/settings/account`** 被语言切换器复用。该接口只挂 `authenticateToken`
   （不限 userType），三端账号都能调，本次没动它的鉴权。
3. **CompanySettings 的字段读取收窄**：原来把接口返回的所有字段 spread 进表单，
   保存时会把 `language` 按页面加载时的旧值写回去，正好会**覆盖掉用户刚切的语言**。
   改成只挑 `CompanyInfo` 声明过的 8 个字段。
4. **Billing 求和 bug 顺带修了**：`sum + p.amount` 在后端返回字符串时会变成字符串拼接
   （踩坑 002）。因为本次把金额显示改成 `formatMoney`，不修的话金额会更明显地错，属于同一改动范围。
5. **确认不受影响**：admin 运营端、customer-portal 本次**一行未动**；
   数据库**零改动**（`users.language` 字段本来就有）；OSS / 邮件 / 权限体系均未触及。

## 🔍 客户门户接口字段核对结果（第 2 次对话，翻译前先做）

| 页面 | 接口 | 结论 |
|---|---|---|
| 我的订单 / 我的报价 / 询价 / 账单 / CMR / 成员管理 / 仪表板 | — | ✅ 字段对得上 |
| **在途跟踪** | `/gps/active` | ❌ 9 个字段无一命中，整页空白 → 已修 |
| **清关操作** | `/customs` | ❌ 除 status 外全错 + 状态映射用小写 → 已修 |
| CMR 文件 | `/cmr` | ❌ sign_status 映射错，3 个状态露英文 → 已修 |
| 账单 | `/finance/receivables` | ❌ 付款状态枚举有 3 个库里没有的值 → 已修 |
| 账户设置 | `/system/settings/account` | ❌ 姓名从来存不进去（发 name，后端要 displayName）→ 已修 |

**后端没有数据源、本次去掉的 UI**（Frank 2026-08-03 拍板「缺数据的列删/隐藏」）：
- 跟踪页的车牌、司机（在 `carrier_vehicles`，`/gps/active` 没 JOIN）
- 跟踪页的进度百分比（没有任何地方计算过）
- 清关页的报关单号、清关类型、起运地（表里不存在）

要恢复这些得改后端接口，属于独立任务。

## 🔍 补丁轮：接口字段全量核对结果（2026-08-03）

Frank 在生产上看到「运输任务」整列空白，逐个接口核对了承运商门户 6 个数据页：

| 页面 | 接口 | 核对结果 |
|---|---|---|
| 概览 Dashboard | `/dashboard/carrier` | ✅ 本来就做了 snake_case 映射 |
| 运输任务 | `/orders` | ❌ 5 个字段全错 → **已修** |
| 上传CMR | `/cmr` + `/orders` | ✅ 本来就用 snake_case |
| 费用结算 | `/finance/payables` | ❌ 除金额外全错 + 汇总恒 0 → **已修** |
| GPS上报 | 纯表单 | ✅ 无列表 |
| 公司设置 | `/system/settings/account` | ❌ 既有 bug，见下方"没改的问题" |

**给后面两端的提醒**：customer-portal 和 admin 做 i18n 时，
**每个列表页都要先把接口 SQL 拉出来核对列名**，别只翻文案。
详见 `docs/踩坑经验库/033`。

### 管理端统计接口全量核对（7 个 /stats 全扫了一遍）

| 接口 | 结论 |
|---|---|
| `/cmr/stats` | ❌ 前端读 `signed`，后端返回 `completed` → 已修 |
| `/customs/stats` | ❌ 前端读 `exception`，后端返回 `exceptions` → 已修 |
| `/shipping-release/stats` | ❌ 前端读 `pending_mail`/`mailed`，后端返回 `original_pending`/`original_sent` → 已修 |
| `/inquiry/stats` | ✅ 对齐 |
| `/quotation/stats` | ✅ 对齐（`conversion_rate` 是后端算好补进响应的） |
| `/contact/stats`、`/user/stats` | ✅ 页面未用 Stats 类型，无错配 |

> 有个值得记的现象：把 interface 改对之后，`tsc` **立刻抓到了一处漏改的初始值**。
> 说明让类型定义对齐真实契约本身就能让编译器帮上忙——写错时它反而完全沉默。

### ⚠️ CI 部署管线（本次踩坑 034）

- 仓库有 GitHub Actions `Deploy to Production`，push 到 main 自动构建 + rsync + 重启 PM2
- **根 `workspaces` 里没有 `carrier-portal`**，CI 只在根跑一次 `npm install`，
  所以它的依赖从来没装过。以前靠依赖提升借到了，加 `i18next` 后立刻炸
- 已在 CI 里补 `cd carrier-portal && npm ci`
- **CI 是先构建后 rsync**，构建失败时 rsync 根本不执行 → 生产不会被半吊子部署污染，
  但也意味着**那几次提交的后端代码 CI 没传过**
- CI 的 `rsync --delete` 会删掉 `admin/dist/homepage.html`，
  但第十步「恢复官网主页」会拷回来，这个陷阱仓库里已处理
- **以后 push 完要回头看一眼 Actions 是不是绿的**——手工 scp 成功会掩盖 CI 已瘫的事实

## ⚠️ 生产数据问题：两个门户账号的租户绑定是悬空的（2026-08-03 查出）

想登客户门户验收时发现，`siemens` 和 `speedtrans` 的 `linked_entity_id`
都指向**已被删除的公司记录**（2026-08-02 清理测试账号时留下的）。

后果：账号能登进去，但**门户所有页面都是空的**，且接口全部返回 200 无报错。
排查时极易误判成「i18n 改动把页面弄坏了」——实际与代码无关。

Frank 选择在管理端「用户管理」界面自行改绑（方案 1）：
- `siemens` → 关联客户改为 **安百**（27 个订单）
- `speedtrans` → 关联承运商改为 **Eurosped** 或 **SGF**

已立档 `docs/踩坑经验库/035`。

## ⚠️ 本次发现、但没改的既有问题（留给 Frank 决定）

1. **`carrier-portal` 的 `npm run lint` 跑不起来**——目录下没有 ESLint 配置文件，
   脚本一直是坏的（非本次引入）。要不要补一份配置？
2. **`CompanySettings` 页面的公司信息根本存不住**：它读写的是
   `/system/settings/account`，那个接口只认 `email / phone / displayName / language` 四个字段，
   而页面在填 `companyName / taxNumber / bankAccount` 等 8 个字段——
   **除邮箱和电话外，其余 6 个字段填了保存也不会进库，刷新就没了**。
   这是既有 bug，跟 i18n 无关，本次只做了文案翻译没动逻辑。要修的话得让承运商去写
   `carriers` 表，属于独立的一个小任务。

## ⚠️ 坑 / 注意

- 登录接口 `POST /api/v1/auth/login` 原来**不返回** `language`，只有 `GET /auth/profile` 返回 → 需要补
- 数据库状态值是**大写**（`PENDING_REVIEW` 等，踩坑 004），翻译 key 直接用大写枚举，别再手写中文 map
- 三端 `build` 脚本是裸 `vite build`（不做类型检查，踩坑 014），提交前必须手动跑 `npm run build:check`
- **迁移号：115 已被 P8（日志保留策略）占用，P9 若要动库从 116 起**
- **admin 批次别漏 P8 刚拆出来的新文件**（2026-08-03 凌晨上线）：
  `admin/src/types/openApi.ts`（徽章/事件中文名常量，是现成的 i18n 靶子）、
  `admin/src/components/openapi/{ApiKeysPanel,ApiLogsPanel,WebhookDeliveriesPanel}.tsx`、
  `admin/src/pages/OpenApiManagement.tsx`（已从 950 行拆成 134 行的壳）
- 每次开工前先 `git fetch origin && git rebase origin/main`——P8 对话仍在同一仓库推进

## ▶️ 下次从这里继续

当前停在：**三端前端全部三语化完成并已推送上线（最后一笔 `04468e8`），
只剩后端 message / 邮件模板 / Excel 表头这一批。**

下次开工（做后端 i18n）的第一件事：

1. `git fetch origin && git rebase origin/main`（仓库里还有别的对话在推进）
2. 读本文件的「后续对话」和「遗留问题」两节 + `docs/踩坑经验库/032/033/036/037`
3. **样板已经铺好**：`server/modules/auth/routes.js` 的 login 路由是 additive 模式 ——
   响应里在原有 `message` 之外多带一个 `messageCode`，前端按码查语言包、查不到退回
   后端 `message`。这样老前端不受影响，新前端能翻译。
4. **优先做 `/system/master-data/:type/options`**：这是目前前端界面上
   唯一还会出中文的地方（下拉框 label 由后端用 `name_zh` 拼），详见「遗留问题」第 1 条
5. 迁移号从 **116** 起（115 被 P8 日志保留策略占用）
6. 收尾跑 `npm run check:i18n`（三端一起查）+ 各端 `npm run build:check`

### 校验方式（每端翻完都跑一次）

```bash
# 1. 扫残留中文（排除语言包和注释）
grep -rn '[一-龥]' src --include='*.tsx' --include='*.ts' \
  | grep -v '^src/i18n/locales/' | grep -v '//' | grep -v 'console\.'

# 2. 扫「靠中文文案做判断」的隐患（踩坑 032）
grep -rn "includes('" src --include='*.tsx'

# 3. 类型检查 + 构建（build 不做类型检查，必须用 build:check，踩坑 014）
npm run build:check
```

**校验脚本已固化**（第 4 次对话补的）：

```bash
npm run check:i18n          # 三端一起查
node scripts/check-i18n-keys.js admin   # 只查某一端
```

它查三件事：三语 key 集合是否一致、插值占位符是否一一对应、
代码里 `t('a.b')` 用到的 key 是否都有定义。退出码 0 = 通过。

⚠️ 它只认静态字面量。用模板字符串拼的 key（`t(\`status.${x}\`)`）查不到，
脚本会把这些文件单独列出来提醒人工确认——目前三端共 25 个文件走这条路，
对应的命名空间（`status` / `truckStatus` / `role` / `loginError` /
`specialRequirement` 等）已逐个人工核对过是齐的。

## ⚠️ 第 4 次对话发现、但没改的遗留问题（留给 Frank 决定）

### 1. ~~主数据下拉框的 label 是后端用 `name_zh` 拼的~~ ✅ 已在第 5 次对话解决（迁移 116/117）

`GET /system/master-data/:type/options` 在后端这样拼 label：

```js
} else if (type === 'countries') {
  value = row.name_en
  label = `${row.name_en} (${row.name_zh})`   // ← 中文来自后端
} else if (type === 'special-requirements') {
  value = row.name_zh
  label = row.name_zh                          // ← 整个 label 就是中文
}
```

前端已经全部三语化，但这些下拉框（国家 / 币种 / 箱型 / 船司 / 港口 / 特殊要求）
**在英文和德文界面下仍然显示中文**，因为文案是后端给的。

已经做的兜底：特殊要求在前端按值查 `specialRequirement.*` 语言包，查不到才用后端 label。
其余几类没有这层兜底（值域太大，不适合前端写死）。

**已解决**：迁移 116 加 `name_de` 列 + 回填种子 74 行，迁移 117 补运营自己加的
16 行，接口按 `Accept-Language` 选列，三端 `api.ts` 带头。生产 51 行德语名全满。
`md_special_requirements` 的 value 仍是 `name_zh`（见下面第 2 条，那条没动）。

### 2. 两个枚举把中文当值存进库

| 表.列 | 存的值 | 影响 |
|---|---|---|
| `orders.special_requirements` | `无` / `温控运输` / `危险品 ADR` … | 值不能翻，只能翻显示 label |
| `customs_documents.file_type` | `报关单` / `商业发票` … | 同上 |

`md_special_requirements` 明明有 `code` 列（`NONE` / `TEMP_CONTROL` / `ADR`…），
当初却选了存 `name_zh`（种子文件里有注释说明是刻意与前端硬编码对齐的）。

这次只做了显示层翻译，**值原样保留**。要根治得写数据迁移把存量中文换成 code，
再改所有读写点，属于独立任务，风险不低（涉及订单历史数据）。

### 3. `/carriers/match` 不返回 `on_time_rate` 和 `covered_routes`

派单页的「历史准时率」列和「覆盖路线」区块依赖这两个字段，
后端 SQL 里根本没有 —— 准时率永远显示 `-`，覆盖路线区块永远不渲染。
本次只把 interface 标成可选并加了注释，**没有删 UI 也没有补后端**。
要么补后端（准时率需要新的统计口径），要么按客户门户的先例把这两处删掉。

### 4. Dashboard 有 4 处写死的假数据

「+3 vs 昨日」「8 即将到达」「完成率 94.2%」「2 个账单待确认」「1 个运输公司资质到期」
—— 数字都是硬编码的，没有对应接口。本次照原样翻译成三语（**行为未改**），
代码里加了注释标注。要不要接真数据或先隐藏，请 Frank 定。

### 5. 通知邮件的标题/正文仍是中文（第 5 次对话发现，未改）

走通用模板的那类通知邮件，**外壳（页眉/页脚/登录提示）已经三语了，
但标题和正文还是中文**。

原因：`notifications.title` / `.message` 是通知引擎在**写库那一刻**就
渲染好的中文文本，发信时只拿到成品字符串，翻不了。

要根治得把通知表改成存「类型 + 变量」（例如
`type='ORDER_CONFIRMED', payload={orderNo:'ORD001'}`），发信和站内信
两处各自按收件人语言渲染。这会动 `core/notification-engine.js`、
`notifications` 表结构和所有写通知的调用点，属于独立任务。

影响范围有限：报价确认和账单提醒这两类**客户会收到的**邮件走的是专用模板，
已经三语了；走通用模板的主要是发给运营自己人的系统通知。
