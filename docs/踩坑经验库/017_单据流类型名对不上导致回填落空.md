# 017 单据流的 flow_type 不是你写的那个值，显式建连线被静默跳过

## 问题现象

写迁移 107 时要给存量报价补 `converted_order_id`，靠单据流反查"这张报价建出过哪张订单"：

```sql
UPDATE quotations q SET converted_order_id = o.id
FROM document_flow df JOIN orders o ON o.document_id = df.subsequent_doc_id
WHERE df.preceding_doc_id = q.document_id
  AND df.flow_type = 'QUOTATION_TO_ORDER';   -- ← 命中 0 行
```

SQL 语法完全正确、表和列都存在、跑起来不报任何错，**就是一行都更新不到**。

## 根本原因

`order/service.js` 里明明白白写着建 `QUOTATION_TO_ORDER` 连线：

```js
await documentFlow.createFlowLink(client, {
  precedingDocType: 'QUO', subsequentDocType: 'ORD',
  flowType: 'QUOTATION_TO_ORDER',
  amount: orderData.clientPrice, currency: orderData.currency
})
```

但库里实际存的是 **`QUO_TO_ORD`**，而且 `amount` 是 NULL。

原因是**同一条连线被建了两次，第一次赢了**：

1. 前面几行 `documentEngine.createDocument()` 传了 `sourceDocType: 'QUO'`，
   凭证引擎内部会自动建一条连线，类型名是**拼出来的**：
   ```js
   const flowType = `${sourceDocType}_TO_${docType}`   // 'QUO' + '_TO_' + 'ORD'
   ```
2. 随后 `createFlowLink` 想再建一条，撞上唯一约束：
   ```sql
   ON CONFLICT (preceding_doc_id, subsequent_doc_id) DO NOTHING
   ```
   **DO NOTHING 把它整条丢掉了** —— 不只是 flow_type 没改成 `QUOTATION_TO_ORDER`，
   连本该带上的 `amount` / `currency` 也一起没了。

所以那段显式 `createFlowLink` 从上线到现在**一次都没生效过**，
而且因为 `DO NOTHING` 不报错、不返回受影响行数，谁也没发现。

## 怎么发现的

不是靠读代码发现的 —— 读代码只会看到 `flowType: 'QUOTATION_TO_ORDER'` 然后信以为真。
是跑了一次真实的「接受报价 → 自动建单」，然后**把 document_flow 表打出来看**：

```
单据流: [{"flow_type":"QUO_TO_ORD","amount":null}]
```

## 正确做法

迁移里两个值都匹配，将来那句 `createFlowLink` 被修好也不会漏：

```sql
AND df.flow_type IN ('QUO_TO_ORD', 'QUOTATION_TO_ORDER')
```

同时新增 `quotations.converted_order_id` 外键，让"这张报价建没建单"有**唯一事实来源**，
不再依赖反查单据流、也不靠 status 推断。

## 防护规则

1. **凡是靠某个字面值去 WHERE 的迁移/查询，先去库里 `SELECT DISTINCT 那一列` 看真实取值**，
   不要照抄源码里写的常量 —— 中间可能隔着一层拼接或另一个写入方。
2. **`ON CONFLICT ... DO NOTHING` 会静默丢弃整行**。当第二次写入带着第一次没有的信息
   （本例的 amount/currency）时，DO NOTHING 就是在丢数据。这种场景要用
   `DO UPDATE SET x = COALESCE(表.x, EXCLUDED.x)` 补齐，而不是直接放弃。
3. **同一份数据有两个写入方时必须查清谁先谁后**。凭证引擎的 `createDocument`
   传了 `sourceDocType` 就会自动建单据流，业务层不必也不该再建一次。
4. 验证"写进去了吗"要**直接查表**，不能只看接口返回 200。

## 遗留 —— 2026-08-16 已修复

> 原文写于 P3 期间："`order/service.js` 里那段死掉的 `createFlowLink` 本次**没有动**
> （改动影响面超出 P3 范围）。后果是**所有** QUO→ORD 单据流的 `amount`/`currency`
> 都是 NULL，单据流视图上看不到金额。"

2026-08-16 按当时的建议修完了，两头都堵：

1. **金额提前到第一个写入方**：`createDocument` 新增 `flowAmount` / `flowCurrency`
   两个可选参数，传了就写进它自动建的那条连线。`order/service.js` 建订单凭证时
   直接把 `clientPrice` 传进去，步骤 4 那段死代码删除。
2. **ON CONFLICT 改为只补空值**（就是原文建议的做法）：
   ```sql
   ON CONFLICT (preceding_doc_id, subsequent_doc_id) DO UPDATE SET
     quantity = COALESCE(document_flow.quantity, EXCLUDED.quantity),
     amount   = COALESCE(document_flow.amount,   EXCLUDED.amount),
     currency = COALESCE(document_flow.currency, EXCLUDED.currency)
   ```
   **`flow_type` 刻意不进 DO UPDATE**：两个写入方用的名字不一样
   （`QUO_TO_ORD` / `QUOTATION_TO_ORDER`），覆盖会让库里取值在两者之间横跳，
   本文开头那种按字面值 WHERE 的迁移会再次落空。上面"正确做法"里
   `IN ('QUO_TO_ORD','QUOTATION_TO_ORDER')` 的写法**继续保留**。

顺带把同一根因的另外两条连线也带上金额：ORD→FI_AR、ORD→FI_AP
（`order/service.js` 自动生成应收应付处），以及 FI_AR/FI_AP→FI_REC/FI_PAY
（`finance/routes.js` 收付款处，金额是本次收付额、可能是部分付款）。

存量数据由迁移 `132_backfill_document_flow_amount.sql` 回填
（执行时生产只有 1 行：订单 EU-20260808-0004，900.00 EUR）。

**仍未修的**：`INQ_TO_QUO` 和 `QUO_TO_QUO`（报价改版本）两类连线金额依旧是 NULL。
不是遗漏 —— 这两处建凭证的时点上报价总价还没算出来
（`quotation/routes.js` 是先建凭证、后跑定价引擎），要带金额得先调代码顺序，
和本次的"被 DO NOTHING 吃掉"不是同一个问题。

## 涉及文件

| 文件 | 说明 |
|------|------|
| `server/core/document-engine.js` | 传 sourceDocType 就自动建连线，类型名是拼的；2026-08-16 加 `flowAmount`/`flowCurrency` |
| `server/core/document-flow.js` | `createFlowLink` 的 ON CONFLICT；2026-08-16 从 DO NOTHING 改为 COALESCE 补空值 |
| `server/modules/order/service.js` | 步骤 4 那段显式 createFlowLink（死代码）；2026-08-16 删除，金额改由步骤 2 传入 |
| `server/modules/finance/routes.js` | 2026-08-16 收付款凭证补 `flowAmount` |
| `server/database/migrations/107_inquiry_quotation_loop.sql` | 回填改为匹配两个值 |
| `server/database/migrations/132_backfill_document_flow_amount.sql` | 2026-08-16 回填存量空金额 |
